# Handoff prompt: Wix Business Manager → Wix storefront

The Wix Business Manager fills in `<WIX_CLIENT_ID>` (the site's public headless client id),
then the user copies this prompt into any coding agent. It drives the **wix-storefront**
skill — all rules, steps, and gotchas live in that skill's `SKILL.md`, not here.

## Copyable prompt

> Build a storefront for my Wix store using the **wix-storefront** skill in the
> `wix-headless-snippets` repo. My public Wix client id is `<WIX_CLIENT_ID>`.
> Follow the skill's `SKILL.md` end to end.

Fill in `<WIX_CLIENT_ID>` before sharing.
